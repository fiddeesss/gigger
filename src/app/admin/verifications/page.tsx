// Placeholder — Phase 6 (ID + selfie review) replaces this.
export default function AdminVerificationsPage() {
  return (
    <div className="flex flex-col gap-3">
      <h1 className="text-lg font-semibold">ID verifications</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        The ID + selfie review queue arrives with the verification build.
      </div>
    </div>
  );
}
