// Placeholder — Phase 6 (user search + standing actions) replaces this.
export default function AdminUsersPage() {
  return (
    <div className="flex flex-col gap-3">
      <h1 className="text-lg font-semibold">Users</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        User search and account standing arrive with the profile build.
      </div>
    </div>
  );
}
