// Placeholder — Phase 5 (manual payout queue) replaces this.
export default function AdminRedemptionsPage() {
  return (
    <div className="flex flex-col gap-3">
      <h1 className="text-lg font-semibold">Payouts</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        The manual payout queue (GCash / Maya / load) arrives with the wallet build.
      </div>
    </div>
  );
}
