// Placeholder — Phase 6 (profile, tiers & verification) replaces this.
export default function ProfilePage() {
  return (
    <div className="flex flex-col gap-3 px-4 py-4">
      <h1 className="text-[18px] font-semibold">Profile</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        Your tiers, verification and account standing will live here — coming
        with the profile build.
      </div>
    </div>
  );
}
