// Placeholder — Phase 5 (wallet + manual redemption) replaces this.
export default function WalletPage() {
  return (
    <div className="flex flex-col gap-3 px-4 py-4">
      <h1 className="text-[18px] font-semibold">Wallet</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        Balance, redemption and payout receipts will live here — coming with the
        wallet build.
      </div>
    </div>
  );
}
