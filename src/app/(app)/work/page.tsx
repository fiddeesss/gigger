// Placeholder — Phase 4 (review engine + My Work) replaces this.
export default function WorkPage() {
  return (
    <div className="flex flex-col gap-3 px-4 py-4">
      <h1 className="text-[18px] font-semibold">My work</h1>
      <div className="rounded-xl bg-surface p-4 text-[13px] text-neutral-500 shadow-sm">
        Your submissions and their review status will live here — coming with
        the review engine.
      </div>
    </div>
  );
}
